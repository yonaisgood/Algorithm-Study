function solution(arr, idx) {
    let answer = 0;
    
    for(let i = idx; i < arr.length; i++){
        let cur = arr[i];
        if(cur == 1)
            return i
        }
    return -1;       
    }
    

